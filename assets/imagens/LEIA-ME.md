# Imagens — Parte 3 (Dados Visuais)

Este repositório versiona apenas uma **amostra leve** (~12 imagens) em
`assets/imagens/amostras/`, só para ilustrar o `README.md` e o documento resumo.

O conjunto completo (mínimo de 100 imagens, exigido pelo enunciado da Fase 1) **não
fica no Git** — conforme a regra inviolável 2 do `CLAUDE.md`/`AGENTS.md` (nada acima
de ~5 MB e nenhum lote de imagens no repositório). Ele está hospedado publicamente
em serviço externo (Drive/OneDrive) e o link de acesso está registrado no
`README.md` da raiz e em `document/datasets/README.md`.

> ⚠️ TODO(humano): colar aqui o link público do conjunto completo de imagens (Drive/OneDrive),
> assim que estiver hospedado, e replicar o mesmo link no `README.md` e em `document/datasets/README.md`.

O rótulo (categoria), a origem e as dimensões de cada imagem — amostra e conjunto
completo — estão descritos no `manifest.csv` (a ser criado em etapa futura desta
mesma fase, junto com a seleção das imagens).
