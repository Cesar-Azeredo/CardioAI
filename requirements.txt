# Versoes travadas para Python 3.12 (ver secao 7 do CLAUDE.md/AGENTS.md).
# numpy fixado exatamente na versao do runtime padrao do Google Colab (2.0.2)
# em 2026-08-27. pandas travado abaixo de 3.0 deliberadamente: pandas 3.0 traz
# breaking changes (copy-on-write por padrao, dtype de string) e este repo tem
# mais 6 fases e varias pessoas mexendo — nao vale o risco so por estar mais
# recente. As demais foram testadas e instaladas via wheel (sem compilar do
# fonte) contra esse par numpy/pandas, no mesmo Python 3.12 — nao ha
# confirmacao de que sejam identicas ao que o Colab tem hoje para elas.
numpy==2.0.2
pandas>=2.2,<3
matplotlib==3.11.1
openpyxl==3.1.5
ucimlrepo==0.0.7
Pillow==12.3.0
jupyter==1.1.1

# Usados por scripts/fase-01/04_prepara_textos.py (coleta e extracao dos
# textos da Parte 2). beautifulsoup4 ja vinha transitivo via jupyter/nbconvert;
# fixado aqui como dependencia direta porque o script importa bs4 diretamente.
requests==2.34.2
beautifulsoup4==4.15.0

# Fase 2 — TF-IDF e classificador de risco (notebooks/fase-02/).
# scikit-learn fixado na versao do runtime padrao do Google Colab, conferida no
# pip-freeze oficial (github.com/googlecolab/backend-info, commit de
# 2026-09-21): scikit-learn==1.6.1, scipy==1.16.3. Wheel cp312 confirmado.
# scipy fixado tambem, embora seja transitivo: sem pin o pip resolve scipy
# 1.18.x, que o sklearn 1.6.1 nao acompanha — LogisticRegression passa a
# emitir "OptimizeWarning: Unknown solver options: iprint" em todo fit.
scikit-learn==1.6.1
scipy==1.16.3
#
# numpy segue em 2.0.2, embora o Colab ja esteja em 2.1.3 (mesmo pip-freeze).
# Regressao de 2026-09-23: scripts 01-05 da Fase 1 rodados em venv limpa com
# numpy 2.1.3 geram CSV, .txt, PROVENIENCIA, manifest, as 12 amostras e as 120
# selecionadas byte-identicos aos versionados — exceto o .xlsx, cujo unico
# arquivo interno diferente e docProps/core.xml (carimbo de data/hora que o
# openpyxl grava). O controle com 2.0.2 diverge do mesmo jeito, ou seja, nao e
# efeito do numpy; mas o criterio acordado foi "byte-identico ou nao sobe".
